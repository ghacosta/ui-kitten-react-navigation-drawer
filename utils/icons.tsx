import React from "react";
import { Icon } from "@ui-kitten/components";

export const HomeIcon = (props: any) => <Icon {...props} name="home" />
export const CubeIcon = (props: any) => <Icon {...props} name="cube" />
export const MenuIcon = (props: any) => <Icon {...props} name="menu" />
export const PowerIcon = (props: any) => <Icon {...props} name="power" />